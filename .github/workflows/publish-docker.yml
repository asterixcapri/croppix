name: Publish Docker image

on:
  release:
    types: [published]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (for reading version)
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Read package version
        id: vars
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if [[ "$VERSION" == *"-"* ]]; then
            echo "is_prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            asterixcapri/croppix:${{ steps.vars.outputs.version }}
            ${{ steps.vars.outputs.is_prerelease == 'false' && 'asterixcapri/croppix:latest' || '' }}

      - name: Build and push Lambda Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.lambda
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            asterixcapri/croppix:${{ steps.vars.outputs.version }}-lambda
            ${{ steps.vars.outputs.is_prerelease == 'false' && 'asterixcapri/croppix:lambda-latest' || '' }}
